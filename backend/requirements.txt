# RDAT Copilot — Local Backend
# Python dependencies for the FastAPI server

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
aiosqlite>=0.20.0
httpx>=0.27.0
pydantic>=2.9.0

# Dev dependencies (also used in CI)
ruff>=0.8.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
